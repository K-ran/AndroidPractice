<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MyAppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@color/customWhite</item>
        <item name="android:colorBackgroundCacheHint">@color/customWhite</item>

    </style>

    <style name="myListTextBoxStyle">
        <item name="android:textSize">@dimen/text_size_list</item>
        <item name="android:textColor">@color/textColor</item>
        <item name="android:gravity">center</item>
        <item name="android:typeface">serif</item>
        <item name="android:padding">@dimen/text_padding</item>
        <item name="android:opacity">translucent</item>
        <item name="android:textStyle">bold</item>
        <item name="android:layout_margin">5dp</item>
    </style>
    <style name="myButtonStyle">
        <item name="android:background">@drawable/custom_button</item>
        <item name="android:layout_margin">3dp</item>
    </style>

    <style name="iphone_style">
        <item name="android:gravity">center</item>
        <item name="android:padding">5dp</item>
        <item name="android:background">@drawable/iphone_button</item>
    </style>
    <style name="iphone_style.iphone_style_button">
        <item name="android:textColor">@color/green</item>
    </style>
</resources>
